crate::ix!();

#[inline] pub fn convert_to_vec(input: &protobuf::RepeatedField<i64>) -> Vec<i64> {
    
    todo!();
    /*
        std::vector<int64_t> out;
      out.reserve(in.size());
      for (const auto d : in) {
        out.push_back(d);
      }
      return out;
    */
}
