crate::ix!();

#[inline] pub fn get_blob1st_dim_size(shape_info: &ShapeInfo) -> i64 {
    
    todo!();
    /*
        if (shape_info.shape.dims_size() == 0) {
        return 0;
      } else {
        return shape_info.shape.dims(0);
      }
    */
}
