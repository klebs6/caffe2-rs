crate::ix!();

#[inline] pub fn sigmoid(x: f32) -> f32 {
    
    todo!();
    /*
        if (x >= 0) {
        return 1. / (1. + exp(-x));
      } else {
        const float exp_x = exp(x);
        return exp_x / (1 + exp_x);
      }
    */
}
