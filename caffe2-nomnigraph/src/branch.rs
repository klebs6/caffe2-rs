crate::ix!();

pub struct Branch {
    base: Terminator,
}

impl Default for Branch {
    
    fn default() -> Self {
        todo!();
        /*
            : Terminator(Instruction::Opcode::Branch
        */
    }
}
