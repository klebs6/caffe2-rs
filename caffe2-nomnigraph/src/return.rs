crate::ix!();

pub struct Return {
    base: Terminator,
}

impl Default for Return {
    
    fn default() -> Self {
        todo!();
        /*
            : Terminator(Instruction::Opcode::Return
        */
    }
}
