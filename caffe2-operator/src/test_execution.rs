crate::ix!();

#[inline] pub fn test_execution(net: &mut Box<NetBase>, num_ops: i32)  {
    
    todo!();
    /*
        // Run 100 times
      for (int i = 0; i < 100; i++) {
        counter.exchange(0);
        net.get()->Run();
        ASSERT_EQ(num_ops, counter.load());
      }
    */
}
