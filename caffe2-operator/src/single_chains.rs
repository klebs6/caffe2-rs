crate::ix!();

#[inline] pub fn single_chains(nodes: &mut Vec<OperatorNode>) -> ExecutionChains {
    
    todo!();
    /*
        ExecutionChains chains;
      for (int i = 0; i < (int)nodes.size(); ++i) {
        chains[i] = {i};
      }
      updateOperatorNodes(nodes, chains);
      return chains;
    */
}
