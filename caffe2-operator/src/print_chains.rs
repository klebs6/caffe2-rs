crate::ix!();

#[inline] pub fn print_chains(chains: &ExecutionChains)  {
    
    todo!();
    /*
        for (const auto kv : chains) {
        std::stringstream ss;
        ss << kv.first << ": ";
        for (const auto& v : kv.second) {
          ss << v << ", ";
        }
        LOG(INFO) << ss.str();
      }
    */
}
